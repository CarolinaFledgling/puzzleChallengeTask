export interface Card {
  id: number;
  bgColor: string;
  bgColorShape: string;
  isCircle: boolean;
  isSquare: boolean;
  isClick: boolean;
  isTriangle: boolean;
  isMouseEnter: boolean;
  isMouseLeave: boolean;
}
